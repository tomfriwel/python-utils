# python-utils
My Python utils resp.


I create a virtual environment by [virtualenv](https://virtualenv.pypa.io/en/stable/#).

Folder structure:

```shell
.
├── bin
├── lib
└── utils
```

`utils` is the resp.